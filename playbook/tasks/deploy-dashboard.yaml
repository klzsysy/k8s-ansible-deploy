---
  - hosts: localhost
    connection: local
    gather_facts: no
    tasks:
      - name: import dashboard yaml
        shell: kubectl create -f ../template/yml_template/kubernetes-dashboard.yaml
        ignore_errors: true
